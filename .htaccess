RewriteEngine On

# If request is already for an existing file/dir in project root, serve it directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise route everything through the front controller in public/
RewriteRule ^ public/index.php [QSA,L]
